name: mp_business
description: Local Melos placeholder for mp_business (real app lives in a separate repo).
publish_to: none
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  muzhiki_dependencies:
    path: ../../packages/muzhiki_dependencies
  muzhiki_ui:
    path: ../../packages/muzhiki_ui
  muzhiki_bridge:
    path: ../../packages/muzhiki_bridge
  muzhiki_support:
    path: ../../packages/muzhiki_support
  muzhiki_report_problem:
    path: ../../packages/muzhiki_report_problem

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
