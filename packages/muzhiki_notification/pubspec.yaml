name: muzhiki_notification
description: Push permission dialog and first-auth notification flow for Muzhiki apps.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  muzhiki_dependencies:
    path: ../muzhiki_dependencies
  muzhiki_ui:
    path: ../muzhiki_ui
  flutter_screenutil: ^5.9.3
  go_router: ^17.3.0
  permission_handler: ^12.0.3
  app_settings: ^6.1.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_lints: ^6.0.0
